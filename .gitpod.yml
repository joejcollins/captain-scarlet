image:
  file: .gitpod.Dockerfile

tasks:
  - init: sudo usermod --password $(openssl passwd -1 $RSTUDIO_PASSWORD) gitpod

ports:
  - port: 8787
    onOpen: open-browser
  
vscode:
  extensions:
    - eamodio.gitlens
    - streetsidesoftware.code-spell-checker
    - ms-python.python
    - Ikuyadeu.r
